import { useState } from "react";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      {/* The main app will run through the Router in main.jsx */}
      {/* This component is just a placeholder */}
    </div>
  );
}

export default App;
